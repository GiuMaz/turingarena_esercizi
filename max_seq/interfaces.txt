interface max_seq {

    function solve(int[] v, int n) -> int;

    main {

        var int N;
        var int[] A;

        input N;

        alloc A : N;
        for(i : N) {
            input A[i];
        }

        var int s;
        call solve(A, N) -> s;
        output s;
    }
}
